{
  "name": "weather",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "npm run build && nodemon index.js -e .html, js, css, jpg, png",
    "build": "esbuild src/scripts/main.js  --bundle --minify src/css/style.css --outdir=public"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "express": "^4.18.2",
    "nodemon": "^2.0.22"
  },
  "devDependencies": {
    "esbuild": "^0.17.19"
  }
}
